# Copy to .env and adjust for your machine. Never commit .env itself.

# Redirect Hugging Face cache off C:\Users\<you>\.cache to avoid Windows long-path issues
HF_HOME=C:/hf_cache

# Avoid tokenizer fork warnings on Windows
TOKENIZERS_PARALLELISM=false
